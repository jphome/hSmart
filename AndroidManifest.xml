<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jphome.hsmart"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/logo"
        android:label="@string/app_name" >
        <activity
            android:name=".HSmartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_add_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_del_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_query_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_query_sub_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_cmd_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_video_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_video_out_Activity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="jphome.app.app_query_temperature_Activity"
            android:label="@string/app_name" >
        </activity>
        
        <service android:name=".query_service"></service>
        
    </application>

    <!-- 访问 internet 权限 -->
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" >
    </uses-permission>
    <!-- 往 SDCard 写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 在 SDCard 中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

</manifest>